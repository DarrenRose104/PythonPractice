import math
import math as m #m is the alias that can be used when calling on the function in the import
from math import sqrt, tan #this is used to use the specific functions from the import

print(math.sqrt(3))
print(m.sqrt(4))
print(sqrt(5))
print(tan(2))